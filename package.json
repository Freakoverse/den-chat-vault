{
  "name": "den-vault",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "DEN Chat key vault — isolated signing origin (deploy to vault.<your-domain>).",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@noble/hashes": "^1.4.0",
    "@scure/bip32": "^1.4.0",
    "@scure/bip39": "^1.3.0",
    "nostr-tools": "^2.7.0"
  },
  "devDependencies": {
    "typescript": "~5.6.3",
    "vite": "^5.4.0"
  }
}
